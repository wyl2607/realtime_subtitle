# 开发/测试依赖（跑测试才需要，普通用户装不装都行）
#
# 为什么单独一个文件：requirements.txt 是 install.ps1 给最终用户装的，
# 里面每一项都是程序跑起来必需的；pytest 只有改代码的人用得到，
# 塞进去会让每台用户机器白装一份。
#
# 用法：venv\Scripts\python -m pip install -r requirements-dev.txt

pytest>=9.1.1      # CLAUDE.md 第 4 节第 15 条要求改完跑全量套件；pytest.ini 里
               # 已经把三个独立脚本套件排除出收集，直接 pytest 即可
ruff==0.16.3       # 静态检查，规则集只有 E9+F（为什么这么窄见 ruff.toml）
               # ☠️ 版本必须钉死、且和 .github/workflows/ci.yml 里的那行一致：
               # ruff 每个小版本都可能给已有规则加新检查，本地和 CI 版本不同
               # = 本地全绿但 CI 红，而且看不出为什么。改版本要两处一起改。
               # 用法：venv\Scripts\python -m ruff check .
